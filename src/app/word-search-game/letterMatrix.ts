import { Letter } from './letter';

export class LetterMatrix{
    matrix = [];
    
    createMatrix(words:string[] , matrixWidth:number, matrixHeight:number):any{
        console.log(words);
        console.log(matrixWidth + ' : ' + matrixHeight);
        return [];    
    }
}